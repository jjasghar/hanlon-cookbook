#!/bin/sh -e
# Start puma service

. /etc/profile

# Redirect stderr so everything ends up in the log file
exec 2>&1

cd "/opt/hanlon/embedded/hanlon/web/"

# Start puma
exec bundle exec puma -p 8026
